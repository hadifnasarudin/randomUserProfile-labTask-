name: random_user_profile
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  animated_splash_screen: ^1.3.0
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  lottie: ^2.7.0

flutter:
  uses-material-design: true
  assets:
    - assets/animations/Animation - 1745162662456.json
